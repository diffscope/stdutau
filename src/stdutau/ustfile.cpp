#include "ustfile.h"

#include <fstream>

namespace Utau {

    UstFile::UstFile() = default;

    // TODO: Implement

    bool UstFile::read(std::istream &is) {
        return true;
    }

    bool UstFile::write(std::ostream &os) const {
        return true;
    }
}